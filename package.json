{
    "name": "mission-control",
    "version": "1.0.0",
    "private": true,
    "description": "Mission Control — Exam Prep Platform (Monorepo)",
    "scripts": {
        "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\"",
        "dev:backend": "npm --prefix mission-backend run start:dev",
        "dev:frontend": "npm --prefix mvp run dev",
        "install:all": "npm --prefix mission-backend install && npm --prefix mvp install",
        "build": "npm --prefix mission-backend run build && npm --prefix mvp run build",
        "db:migrate": "npm --prefix mission-backend run prisma:migrate",
        "db:generate": "npm --prefix mission-backend run prisma:generate"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}